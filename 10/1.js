var modifyString = function (s) {
  let chars = [...s], _chars = [...s];

  for (let j = 0; j < _chars.length; j++) {
    let _char = _chars[j];
    if (_char === '?') {
      for (let i = 0; i < 26; i++) {
        let char = String.fromCharCode(i + 97);
        let isFlag = false;
        if (chars[j - 1] !== char) {

          if (j + 1 < _chars.length) {
            if (chars[j + 1] !== char)
              isFlag = true;
          } else {
            isFlag = true;
          }
        }
        if (isFlag) {
          s = s.replace('?', char);
          chars = [...s];
          break;
        }
      }
    }
  }
  return s;
};
modifyString("????????????????????????????????????????????????????????????????????????????????????????????????????"); ``